name: Scheduled build
on:
  schedule:
    - cron: '10 8,14 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Trigger our build webhook on Netlify
      env:
        TOKEN: ${{ secrets.NETLIFY_BUILD_DAILY  }}
      run: curl -X POST -d {} https://api.netlify.com/build_hooks/${TOKEN}
